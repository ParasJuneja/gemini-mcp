{
  "name": "gemini-mcp",
  "version": "1.0.0",
  "description": "MCP server that preprocesses large-payload tasks through Gemini",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@google/generative-ai": "latest",
    "@modelcontextprotocol/sdk": "latest",
    "dotenv": "^16.0.0",
    "ignore": "^5.3.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
